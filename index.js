// uba-fast-lint-config
// Hybrid linting: Biome (formatter) + oxlint (bulk lint) + ESLint (gap plugins)
// Placeholder - will be populated by task #6

export const ubaFastLintConfig = {};
export const generateLintConfig = () => ({});
export const generateLintConfigByFeatures = () => ({});
export const generateFormatterConfig = () => ({});
